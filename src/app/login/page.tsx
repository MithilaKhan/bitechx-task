import Login from '@/components/features/login';
import React from 'react';

const loginPage = () => {
    return (
        <div>
            <Login />
        </div>
    );
};

export default loginPage;